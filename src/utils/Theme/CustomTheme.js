const useTheme = () => {
    const color = {
        primary: "#2CAEE2",
        secondary: "#14457B",
        primaryLight: "#D3E9FB",
        accent: "#000000",
    }
    return { color };
};

export default useTheme;